# Copy this file to .env (same folder) before first run.
# docker-compose.yml AND config/database.php both read these same values,
# so the app and the database container can never drift out of sync.

DB_HOST=127.0.0.1
DB_PORT=3307
DB_NAME=kiln_auth
DB_USER=root
DB_PASS=change_this_local_password
